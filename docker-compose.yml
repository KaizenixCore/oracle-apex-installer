cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  apex-web-installer:
    build: .
    container_name: apex-web-installer
    ports:
      - "8888:8888"
    environment:
      - TERM=xterm-256color
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
    restart: unless-stopped
EOF
